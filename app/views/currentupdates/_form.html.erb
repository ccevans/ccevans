<div class="panel-heading">
    <h1>Add Update</h1>
  </div>

<div class="panel-body">
  <div class="panel panel-default">
    <%= form_for @currentupdate, html: { multipart: true } do |f| %>
      <% if @currentupdate.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@currentupdate.errors.count, "error") %> prohibited this post from being saved:</h2>

          <ul>
          <% @currentupdate.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
          <% end %>
          </ul>
        </div>
      <% end %>

      <div class="form-group">
        <%= f.label :info %>
        <%= f.text_field :info, class: "form-control" %>
      </div>
      <div class="form-group">
        <%= f.submit class: "btn btn-primary" %>
      </div>
    <% end %>
  </div>
</div>